/** Selector for the launcher's header layout. `KeepManifest` adds the
 *  `#__NEXUS_PAYLOAD_MANIFEST__ … app:<size>` block (opt-in via
 *  `--remain-manifest`); `StripManifest` emits the minimal single-payload
 *  form used by default.
 */
export type ManifestMode = KeepManifest | StripManifest

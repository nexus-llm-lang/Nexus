/**
 * codegen/intrinsics/memo: memo-target helpers.
 *
 * The `name -> slot` index consumed on the per-call hot path is built once at
 * module-assembly time by `build_memo_target_map` in
 * src/backend/codegen/ctx/memo.nx and stored in CodegenCtx as an O(1) map.
 */

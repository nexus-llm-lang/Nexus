[package]
name = "nexus_fs_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
no_alloc_export = []
component = []

[dependencies]
nexus_wasm_alloc = { path = "../wasm_alloc" }

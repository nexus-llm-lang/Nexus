type Option<T> = Some(value: T) | None

let unwrap_or = fn (opt: Option<i64>, default: i64) -> i64 do
  return if let Some(value: v) = opt then v else default end
end

let main = fn () do
  let x = unwrap_or(opt: Some(value: 42), default: 0)
  let y = unwrap_or(opt: None, default: 99)
end

/** Fixture for nexus-elxhg: sibling of `dup_import_a.nx`. Exports a value
    `dup_val` with the same bare name; importing both selectively in one
    scope must be diagnosed as ambiguous (E4006). Also exports `other_val`
    (a non-colliding name) used by the positive control test. */

export let dup_val = fn () -> i64 do return 200 end

export let other_val = fn () -> i64 do return 200 end

/** Fixture: exports an opaque enum used by regression tests that verify
    opaque-type comparability is rejected outside the defining module. */

export opaque type Color = Red | Blue

export let make_red = fn () -> Color do
  return Red
end

export let make_blue = fn () -> Color do
  return Blue
end

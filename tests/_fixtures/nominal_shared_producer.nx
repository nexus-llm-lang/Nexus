/** Fixture for module-qualified nominal identity: imports the SHARED enum
    `Shared` from `nominal_shared_type.nx` and produces a value of it. The
    declaring module of `Shared` (resolved via the import edge) is the same one
    `consume` resolves to, so the produced value stays assignable to `consume`. */

import { Shared, SWrap } from "tests/_fixtures/nominal_shared_type.nx"

export let produce = fn () -> Shared do
  return SWrap(v: 7)
end

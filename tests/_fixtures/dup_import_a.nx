/** Fixture for nexus-elxhg: exports a value `dup_val` whose bare name also
    appears in `dup_import_b.nx`, so selectively importing both in one scope
    must raise an ambiguous-import diagnostic (E4006). */

export let dup_val = fn () -> i64 do return 100 end

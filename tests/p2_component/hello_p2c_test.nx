// Smoke-test: a minimal Nexus program compiled with --p2-component produces
// a valid WASI preview2 component that passes wasm-tools validate and
// runs under wasmtime run -W component-model=y.
let main = fn () -> unit do
  return ()
end

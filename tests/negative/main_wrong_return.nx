// expect-fail: E2004
// expect-msg: must return
//
// The entry point `main` must have the shape
// `let main = fn () -> unit ...` (or accept `args: [string]`). Returning
// any other type (here `i64`) trips the dedicated `MainNotUnit` arm so
// the diagnostic includes the exact recovery hint.

let main = fn () -> i64 do
  return 0
end

// expect-fail: E2007
// expect-msg: never consumed
//
// nexus-eai4: an array literal [|...|] introduces a linear binding (q=1).
// Letting it fall out of scope without consuming it must raise LinearUnused.

let main = fn () -> unit do
  let a = [| 1, 2, 3 |]
  return ()
end

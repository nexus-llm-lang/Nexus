// expect-fail: E4004
// expect-msg: file not found
//
// `import "std:NAME"` resolves to `nxlib/stdlib/NAME.nx`. A non-existent
// module name surfaces as the loader-side "file not found" diagnostic —
// double-check the module path and that the relevant `.nx` ships in the
// runtime's stdlib bundle.

import { foo } from "std:this_module_does_not_exist"

let main = fn () -> unit do
  return ()
end

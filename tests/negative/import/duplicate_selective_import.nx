// expect-fail: E4006
// expect-msg: ambiguous import
// expect-msg: dup_val
//
// nexus-elxhg: two selective imports in the same scope binding the same bare
// name to two different module targets must be diagnosed, not silently
// resolved to the last-imported one. Previously this printed 200 (b's val)
// with exit 0; now it must fail at import resolution with an ambiguity error
// and the suggestion to disambiguate with `as`.

import { dup_val } from "tests/_fixtures/dup_import_a.nx"
import { dup_val } from "tests/_fixtures/dup_import_b.nx"

let main = fn () -> unit do
  return ()
end

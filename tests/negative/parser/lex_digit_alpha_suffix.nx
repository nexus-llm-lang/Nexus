// expect-fail: E1001
// expect-msg: invalid numeric literal
//
// `3abc` must be rejected at lex time: digit-run `3` directly abutting
// letter `a` with no separator is invalid.

let main = fn () -> unit do
  let _x = 3abc
  return ()
end

// expect-fail: E1001
// expect-msg: invalid numeric literal
//
// `0b1` must be rejected at lex time: digit-run `0` directly abutting `b`
// with no separator is invalid.  Binary literal support is not implemented.

let main = fn () -> unit do
  let _x = 0b1
  return ()
end

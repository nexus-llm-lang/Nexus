// expect-fail: E1001
// expect-msg: invalid numeric literal
//
// `1_000` must be rejected at lex time: digit-run `1` directly abutting `_`
// with no separator is invalid.  Digit-separator support is not implemented.

let main = fn () -> unit do
  let _x = 1_000
  return ()
end

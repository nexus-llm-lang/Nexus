// expect-fail: E1001
// expect-msg: unterminated block comment
//
// An unterminated /* block comment causes the lexer to emit a clear
// "unterminated block comment" diagnostic (nexus-lhci) pointing at the
// opening `/*`, rather than silently consuming the rest of the file and
// producing a confusing "no main function" error downstream.

/* this block comment is never closed
let main = fn () -> unit do
  return ()
end

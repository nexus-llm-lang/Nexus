// expect-fail: E1001
// expect-msg: parse error
//
// A double-quoted string literal must close on the same line (no implicit
// line continuation). The parser reports the unterminated literal at the
// next statement boundary.

let main = fn () -> unit do
  let _ = "unterminated
  return ()
end


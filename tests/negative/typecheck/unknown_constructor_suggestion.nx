// expect-fail: E2025
// expect-msg: did you mean
//
// "did you mean?" suggestion for UnknownConstructor (nexus-bpjd): when a
// constructor reference cannot be resolved, the typechecker collects all
// in-scope variant names, computes Levenshtein distances, and suggests up
// to 3 candidates with distance <= 2. `Fals` is 1 edit from `False`
// (a built-in boolean variant), so the error should include a suggestion.

type Color = Red | Green | Blue

let main = fn () -> unit do
  Gren
  return ()
end

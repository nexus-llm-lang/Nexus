// expect-fail: E2014
// expect-msg: did you mean
//
// "did you mean?" suggestion for UnknownThrowVariant (nexus-bpjd): when a
// throws row contains an unknown exception name, the typechecker suggests
// near-matches from declared exception groups and their members. `Exnn` is
// 1 edit from `Exn` (the universal exception sentinel), so a suggestion
// should appear.

let foo = fn () -> unit throws { Exnn } do
  return ()
end

let main = fn () -> unit do
  return ()
end

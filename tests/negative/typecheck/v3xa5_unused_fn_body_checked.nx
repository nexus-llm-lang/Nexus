// expect-fail: E2001
// expect-msg: type mismatch
//
// nexus-v3xa.5: D-Let-Top requires every top-level let to be body-checked,
// including unused functions that DCE removes from the codegen path.
// Before fix: `nexus build` exited 0 silently; the type error in `bad` was
// invisible because DCE ran before typecheck and removed `bad` from the
// function list passed to check_all_functions.
let bad = fn () -> i64 do
  let x: i32 = "string"
  return 0
end
let main = fn () -> unit do return () end

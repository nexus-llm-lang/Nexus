// expect-fail: E2028
// expect-msg: reserved by the type system
// expect-msg: exception
// expect-msg: Exn
//
// D-Exception (docs/spec/type-system-formal.md §D-Exception):
// C ∉ {Exn} — the name "Exn" is the universal exception sentinel used
// by U-Row-Exn / T-Raise as the catch-all row anchor. Allowing a user
// declaration to shadow it would break row-unification invariants.
// The typechecker enforces this at registration time (E2028).
//
// nexus-v3xa.11

exception Exn(code: i64)

let main = fn () -> unit do
  return ()
end

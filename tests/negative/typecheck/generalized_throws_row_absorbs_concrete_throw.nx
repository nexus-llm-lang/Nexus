// expect-fail: E2001
// expect-msg: throw Boom
// expect-msg: generalized

exception Boom

let leak = fn <R>() -> i64 throws R do
  throw Boom
end

let pure_caller = fn () -> i64 throws {} do
  return leak()
end

let main = fn () -> unit do
  let _v = pure_caller()
  return ()
end

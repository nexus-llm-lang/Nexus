// expect-fail: E2001
// expect-msg: throws row mismatch

let f = fn (a: @i64, g: (v: i64) -> unit) -> i64 do
  return @a
end

let di = fn (v: i64) -> unit do
  return ()
end

let boom = fn () -> i64 throws { Exn } do
  throw RuntimeError(val: "x")
end

let main = fn () -> unit throws { Exn } do
  let @t = boom()
  let r = f(a: t, g: di)
  return ()
end

// expect-fail: E2027
// expect-msg: unsupported parameter list
//
// P9 wfMain: the only accepted entry-point shapes are
//   let main = fn () -> unit ...
//   let main = fn (args: [string]) -> unit ...
// Any other parameter list is rejected with E2027.

let main = fn (x: i64) -> unit do
  x
  return ()
end

// expect-fail: E2001
// expect-msg: force: expected lazy type
//
// Negative fixture for nexus-lq5l: forcing a non-lazy (i64) expression
// must be rejected. The unify(τ, @β) path should fail to unify i64 with
// TyLazy(?β) and report the "expected lazy type" error.

let main = fn () -> unit do
  let x = 42
  @x
  return ()
end

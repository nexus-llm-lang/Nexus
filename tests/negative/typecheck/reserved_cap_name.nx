// expect-fail: E2028
// expect-msg: reserved by the type system
// expect-msg: cap
// expect-msg: PermFs
//
// D-Cap (docs/spec/type-system-formal.md §D-Cap):
// X ∉ SysCaps — the name "PermFs" is one of the seven system capability
// names. Allowing a user cap declaration to shadow a system capability
// would make require-row wfCap checks ambiguous (wfCap accepts SysCap
// names unconditionally; a user-declared cap with the same name would
// bypass structural validation). The typechecker enforces this at
// registration time (E2028).
//
// nexus-v3xa.11

cap PermFs do
  fn read_file(path: string) -> string
end

let main = fn () -> unit do
  return ()
end

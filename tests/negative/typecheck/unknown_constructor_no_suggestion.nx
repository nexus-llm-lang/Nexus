// expect-fail: E2025
// expect-msg: unknown constructor
//
// UnknownConstructor (nexus-bpjd): a completely unrelated name yields no
// suggestion but still emits the E2025 error code.

type Shape = Circle | Square

let main = fn () -> unit do
  Hexagon
  return ()
end

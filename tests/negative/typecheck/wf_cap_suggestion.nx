// expect-fail: E2013
// expect-msg: did you mean
//
// "did you mean?" suggestion for UnknownCapability (nexus-bpjd): when a
// require row contains an unknown capability name, the typechecker suggests
// near-matches from system caps and declared caps. `PermConsol` is 1 edit
// from `PermConsole`, so a suggestion should appear.

let main = fn () -> unit require { PermConsol } do
  return ()
end

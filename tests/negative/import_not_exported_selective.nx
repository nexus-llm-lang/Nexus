// expect-fail: E4005
// expect-msg: not exported by module
// expect-msg: two_pow_32
//
// Selective import of a non-exported symbol must be rejected at import time.
// D-Export (type-system-formal.md §8): only names with the `export` prefix are
// visible to importing modules. `two_pow_32` in std:str is a module-private
// implementation detail (no `export` prefix).

import { two_pow_32 } from "std:str"

let main = fn () -> unit do
  return ()
end

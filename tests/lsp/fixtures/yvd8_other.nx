// Fixture for tests/lsp/yvd8_definition_*_test.nx — provides exported `g`
// so the importer-side test can assert that go-to-definition crosses the
// import boundary and lands on the `let g` line below.
export let g = fn (x: i64) -> i64 do
  return x + 1
end

/// Pseudo-random number generation.
///
/// Requires PermRandom capability.
interface random {
    /// Generate a non-negative random 64-bit integer.
    next-i64: func() -> s64;

    /// Generate a random integer in the range [min, max).
    range: func(min: s64, max: s64) -> s64;
}

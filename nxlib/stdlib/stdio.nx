import external [=[nxlib/stdlib/stdio.wasm]=]

pub let print = external [=[print]=] : (val: string) -> unit effect { IO }

pub let println = fn (val: string) -> unit effect { IO } do
  let msg = val ++ [=[\n]=]
  return perform print(val: msg)
endfn

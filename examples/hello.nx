import { Console, system_handler } from nxlib/stdlib/stdio.nx

let main = fn () -> unit require { PermConsole } do
  inject system_handler do
    Console.println(val: [=[👽️ こんにちは, world!]=])
  end
end

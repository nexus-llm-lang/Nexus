// expect-fail: E2001
// expect-msg: type mismatch
//
// Type annotations on `let` bindings are checked against the RHS. A
// string cannot inhabit `i64`. The typechecker reports the mismatch with
// the binding's source span.

let main = fn () -> unit do
  let _: i64 = "not an int"
  return ()
end

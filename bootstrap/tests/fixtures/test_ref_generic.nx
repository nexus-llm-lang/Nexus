
    let box = fn <T>(x: T) -> unit do
        let ~r = x
        let v = ~r
        return ()
    end

    let main = fn () -> unit do
        box(x: 10)
        box(x: true)
        return ()
    end
    
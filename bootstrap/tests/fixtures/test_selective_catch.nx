
    exception Boom(i64)
    exception Timeout(i64)

    let main = fn () -> i64 do
      try
        raise Boom(42)
      catch
        | Boom(code) -> return code
        | Timeout(ms) -> return ms
        | _ -> return -1
      end
      return 0
    end


    exception Boom(i64)
    exception Timeout(i64)

    let main = fn () -> i64 do
      try
        raise Boom(42)
      catch
        case Boom(code) -> return code
        case Timeout(ms) -> return ms
        case _ -> return -1
      end
      return 0
    end

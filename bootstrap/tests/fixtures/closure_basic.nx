let apply = fn (f: fn (i64) -> i64, x: i64) -> i64 do
  return f(x: x)
end

let main = fn () -> unit do
  let inc = fn (n: i64) -> i64 do return n + 1 end
  let result = apply(f: inc, x: 41)
  // result should be 42
end

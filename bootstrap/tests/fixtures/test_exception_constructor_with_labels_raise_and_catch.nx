
    exception Boom(code: i64)

    let main = fn () -> i64 do
      try
        let err = Boom(code: 42)
        raise err
      catch
        | Boom(code: c) -> return c
        | RuntimeError(_) -> return -1
        | InvalidIndex(_) -> return -2
      end
      return 0
    end
    
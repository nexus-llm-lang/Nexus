
    exception Boom(i64)

    let main = fn () -> i64 do
      try
        let err = Boom(42)
        raise err
      catch
        case Boom(code) -> return code
        case RuntimeError(_) -> return -1
        case InvalidIndex(_) -> return -2
      end
      return 0
    end
    
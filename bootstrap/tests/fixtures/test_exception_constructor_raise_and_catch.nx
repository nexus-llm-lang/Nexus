
    exception Boom(i64)

    let main = fn () -> i64 do
      try
        let err = Boom(42)
        raise err
      catch
        | Boom(code) -> return code
        | RuntimeError(_) -> return -1
        | InvalidIndex(_) -> return -2
      end
      return 0
    end
    
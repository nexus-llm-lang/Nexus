// Negative fixture: or-pattern omits the Blue constructor.
// Port of bootstrap/tests/typecheck/exhaustiveness.rs::
// test_or_pattern_missing_constructor_is_non_exhaustive.
type Color = Red | Green | Blue

let main = fn () -> unit do
  let c = Red
  match c do
    | Red | Green -> return ()
  end
end

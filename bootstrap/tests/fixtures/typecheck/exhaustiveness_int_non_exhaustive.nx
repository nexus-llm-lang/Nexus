// Negative fixture: integer match with no wildcard arm.
// Port of bootstrap/tests/typecheck/exhaustiveness.rs::test_int_non_exhaustive.
let main = fn () -> unit do
  let i = 10
  match i do
    | 0 -> return ()
    | 1 -> return ()
  end
end

// Negative fixture: a bare integer literal does not auto-narrow to i32
// without an annotation.
// Port of bootstrap/tests/typecheck/inference.rs::
// test_int_literal_is_not_i32_without_annotation.
let test_fn = fn () -> i32 do
  let x = 1
  return x
end

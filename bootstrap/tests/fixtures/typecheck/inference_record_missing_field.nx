// Negative fixture: accessing a field that does not exist on the record.
// Port of bootstrap/tests/typecheck/inference.rs::test_record_fail.
let main = fn () -> unit do
  let r = { x: 1 }
  let y = r.y
  let _ = y
  return ()
end

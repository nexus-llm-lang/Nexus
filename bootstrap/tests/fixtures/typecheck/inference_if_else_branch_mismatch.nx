// Negative fixture: if/else branches must unify.
// Port of bootstrap/tests/typecheck/inference.rs::
// if_else_expr_type_mismatch_fails (and the proptest negative
// prop_typecheck_if_else_branches_must_match).
let main = fn () -> unit do
  let x: i64 = if true then 1 else "no" end
  let _ = x
  return ()
end

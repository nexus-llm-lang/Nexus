// Negative fixture: argument-type mismatch.
// Port of bootstrap/tests/typecheck/inference.rs::test_arg_mismatch.
// `foo` expects `x: i64` but the call passes a bool.
let foo = fn (x: i64) -> i64 do return x end

let test_fn = fn () -> i64 do
  return foo(x: true)
end

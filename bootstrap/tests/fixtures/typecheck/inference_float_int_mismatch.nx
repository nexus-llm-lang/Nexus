// Negative fixture: int and float operands cannot mix in `+.`.
// Port of bootstrap/tests/typecheck/inference.rs::test_float_int_mismatch.
let main = fn () -> unit do
  let x = 1 +. 2.0
  let _ = x
  return ()
end

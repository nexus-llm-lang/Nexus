// Negative fixture: an unused `@x` thunk must be forced exactly once.
// Port of bootstrap/tests/typecheck/linear.rs::test_lazy_unused_is_error.
let main = fn () -> unit do
  let @x = { id: 1 }
  return ()
end

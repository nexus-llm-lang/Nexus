// Negative fixture: `@T` is never auto-droppable, even when T is a
// primitive. The thunk must be forced.
// Port of bootstrap/tests/typecheck/linear.rs::
// test_lazy_primitive_unused_is_error.
let main = fn () -> unit do
  let @x = 42
  return ()
end

// Negative fixture: `Ok(Err)` arm is missing.
// Port of bootstrap/tests/typecheck/exhaustiveness.rs::
// test_nested_result_non_exhaustive.
let main = fn () -> unit do
  let x: Result<Result<i64, i64>, i64> = Ok(val: Ok(val: 1))
  match x do
    | Ok(val: Ok(val: v)) -> return ()
    | Err(err: e) -> return ()
  end
end

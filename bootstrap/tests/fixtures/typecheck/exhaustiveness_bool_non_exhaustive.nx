// Negative fixture: bool match without `false` arm.
// Port of bootstrap/tests/typecheck/exhaustiveness.rs::test_bool_non_exhaustive.
let main = fn () -> unit do
  let b = true
  match b do
    | true -> return ()
  end
end

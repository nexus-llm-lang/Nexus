// Negative fixture: `@T` is one-shot. Forcing the same thunk twice
// must fail.
// Port of bootstrap/tests/typecheck/linear.rs::
// test_lazy_double_force_is_error.
let main = fn () -> unit do
  let @x = 42
  let a = @x
  let b = @x
  let _ = a
  let _ = b
  return ()
end

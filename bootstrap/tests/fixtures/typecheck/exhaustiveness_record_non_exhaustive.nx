// Negative fixture: record match misses {x:false, _} and {x:true, y:false}.
// Port of bootstrap/tests/typecheck/exhaustiveness.rs::test_record_non_exhaustive.
let main = fn () -> unit do
  let r = { x: true, y: true }
  match r do
    | { x: true, y: true } -> return ()
  end
end

// Negative fixture: bootstrap-side gate — `main throws { Exn }` is
// rejected by the Rust typechecker. The self-host compiler relaxes this
// via the wrap_main_if_needed HIR pass (issue nexus-urf5).
// Port of bootstrap/tests/typecheck/effects.rs::
// test_main_cannot_declare_exn_throws.
let main = fn () -> unit throws { Exn } do
  return ()
end

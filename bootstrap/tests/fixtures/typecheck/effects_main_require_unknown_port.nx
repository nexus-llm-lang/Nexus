// Negative fixture: `main require { ... }` only accepts known PermXxx
// names. A user-defined cap (`Custom`) is rejected.
// Port of bootstrap/tests/typecheck/effects.rs::
// test_main_require_unknown_port_is_rejected.
cap Custom do
  fn foo() -> unit
end
let main = fn () -> unit require { Custom } do
  return ()
end

// Negative fixture: `raise` requires `Exn` in the throws row. Plain
// strings are not raisable values either way.
// Port of bootstrap/tests/typecheck/effects.rs::test_raise_requires_exn.
let fail = fn () -> unit do
  raise "oops"
  return ()
end
let main = fn () -> unit do
  return ()
end

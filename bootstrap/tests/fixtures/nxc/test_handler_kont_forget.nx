cap Gen do
  fn yield(val: i64) -> unit
end

let collector = handler Gen do
  fn yield(val: i64) -> unit with @k do
    let _ = val
  end
end

let main = fn () -> unit do
  inject collector do
    Gen.yield(val: 10)
  end
end

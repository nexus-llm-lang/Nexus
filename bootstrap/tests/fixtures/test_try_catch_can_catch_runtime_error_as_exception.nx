
    let main = fn () -> unit do
      try
        let _ = [| 1 |][10]
      catch
        case RuntimeError(val: _) -> return ()
        case InvalidIndex(val: _) -> return ()
      end
      return ()
    end
    